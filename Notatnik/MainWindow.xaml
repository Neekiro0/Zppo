<Window x:Class="Notatnik.MainWindow"
        xmlns:sys="clr-namespace:System;assembly=mscorlib"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Notatnik"
        mc:Ignorable="d"
        SizeToContent="WidthAndHeight"
        Title="Notatnik" MinHeight="570" MinWidth="800" Foreground="Black"  >

    <Grid Background="#FFFAD980">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <StackPanel VerticalAlignment="Top" Grid.ColumnSpan="2">
            <Rectangle HorizontalAlignment="Stretch" Fill="#FFFFBA00"  Stroke="#FFFFBA00" Height="34" />
            <Rectangle Fill="Black" HorizontalAlignment="Stretch" Height="1"/>
        </StackPanel>

        <Grid Grid.Column="0" Margin="10,10,0,10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <StackPanel Orientation="Vertical" Grid.Row="0">
                <Label Content="Dostępne notatki:" Background="#FFFFBA00" Height="23" Width="188" Margin="0,0,0,5"/>
                <StackPanel Orientation="Horizontal">
                    <ComboBox x:Name="TagFilterComboBox" Width="111" SelectionChanged="TagFilterComboBox_SelectionChanged"
                      IsEditable="True" StaysOpenOnEdit="True" Text="Filtruj po tagu..." Height="23">
                        <ComboBox.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding}"/>
                            </DataTemplate>
                        </ComboBox.ItemTemplate>
                    </ComboBox>
                    <Button Content="Reset" Click="PokazWszystkiePlikiMenuItem_Click"
                    Margin="5,0,5,0" Width="74" Background="#FFD8D9BC" BorderBrush="Black"/>
                </StackPanel>
            </StackPanel>

            <ListBox Grid.Row="1" x:Name="ListaTXT" Margin="0,2,0,5"
             VerticalAlignment="Stretch" SelectionChanged="ListaTXT_SelectionChanged" SelectionMode="Extended">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding FileName}"/>
                            <ItemsControl ItemsSource="{Binding Tags}" Margin="5,0,0,0">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <StackPanel Orientation="Horizontal"/>
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Background="#FFE1F5FE" CornerRadius="3" Margin="1" Padding="2,0,2,0">
                                            <TextBlock Text="{Binding}" FontSize="10" Foreground="#FF01579B"/>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

            <StackPanel Grid.Row="2" Orientation="Vertical">
                <Button x:Name="Odswiezbt" Content="Odśwież" Click="Odswiezbt_Click" Margin="3" BorderBrush="Black"/>
                <Button x:Name="Zmienfolderbt" Content="Zmień folder" Click="Zmienfolderbt_Click" Margin="3" BorderBrush="Black"/>
                <Button x:Name="Nowybt" Content="Utwórz plik" Click="Nowybt_Click" Margin="3" BorderBrush="Black"/>
                <Button x:Name="Usunbt" Content="Usuń zaznaczone" Click="Usunbt_Click" FontWeight="Bold" Margin="3"
                Foreground="Red" BorderBrush="#FFDC2020" />
            </StackPanel>
        </Grid>


        <Grid Grid.Column="1" Margin="5,10,10,10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Label Content="Zawartość notatki:" HorizontalAlignment="Stretch" Background="#FFFFBA00" Height="23" MinWidth="590" Margin="0,0,0,5" />

            <TextBox x:Name="ContentTextBox" Grid.Row="1"
                     TextWrapping="NoWrap"
                     AcceptsReturn="True"
                     VerticalScrollBarVisibility="Auto"
                     HorizontalScrollBarVisibility="Auto"
                     TextChanged="ContentTextBox_TextChanged"
                     VerticalAlignment="Stretch"
                     HorizontalAlignment="Stretch" />

            <StatusBar Grid.Row="2" VerticalAlignment="Bottom">
                <StatusBarItem>
                    <TextBlock x:Name="StatusText" Text="Gotowe"/>
                </StatusBarItem>
                <StatusBarItem BorderBrush="Black">
                    <TextBlock x:Name="EditStatusText" Text="" Foreground="Red" FontWeight="Bold" Width="500"/>
                </StatusBarItem>
            </StatusBar>

            <StackPanel Orientation="Horizontal" Grid.Row="3" HorizontalAlignment="Right" Margin="0,5,0,0">
                <Button x:Name="Otagujbt" Content="Edytuj tagi" Margin="3" Width="71" Click="Otagujbt_Click" BorderBrush="Black"/>
                <Button x:Name="Zapiszbt" Content="Zapisz zmiany" Margin="3" Click="Zapiszbt_Click" BorderBrush="Black"/>
            </StackPanel>
    </Grid>
    </Grid>
</Window>
