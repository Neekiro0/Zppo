<Window x:Class="Notatnik.TaskDetailsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Notatnik"
        mc:Ignorable="d"
        Title="Szczegóły Zadania" Height="450" Width="400"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">

    <Window.Resources>

        <Style x:Key="StandardButton" TargetType="Button">
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Margin" Value="5,0"/>
            <Setter Property="Background" Value="LightGray"/>
            <Setter Property="BorderBrush" Value="Gray"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="1" 
                                CornerRadius="5">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="AccentButton" TargetType="Button" BasedOn="{StaticResource StandardButton}">
            <Setter Property="Background" Value="#FF1E90FF"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="BorderBrush" Value="#FF1080E0"/>
        </Style>

    </Window.Resources>

    <StackPanel Margin="20">

        <TextBlock Text="Tytuł:" Margin="0,0,0,5" FontWeight="SemiBold"/>
        <TextBox x:Name="TytulTextBox" Height="25" Margin="0,0,0,10"/>

        <TextBlock Text="Opis:" Margin="0,0,0,5" FontWeight="SemiBold"/>
        <TextBox x:Name="OpisTextBox" AcceptsReturn="True" TextWrapping="Wrap" Height="80" Margin="0,0,0,10"/>

        <Grid Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0" Margin="0,0,5,0">
                <TextBlock Text="Data:" Margin="0,0,0,5" FontWeight="SemiBold"/>
                <DatePicker x:Name="DataDatePicker"/>
            </StackPanel>
            <StackPanel Grid.Column="1" Margin="5,0,0,0">
                <TextBlock Text="Czas (HH:mm):" Margin="0,0,0,5" FontWeight="SemiBold"/>
                <TextBox x:Name="TimeTextBox" ToolTip="Format: HH:mm, np. 14:30"/>
            </StackPanel>
        </Grid>

        <TextBlock Text="Jak się masz? (opcjonalnie)" Margin="0,0,0,5" FontWeight="SemiBold"/>
        <ComboBox x:Name="PoczucieComboBox" Margin="0,0,0,10" IsEditable="True">
            <ComboBoxItem Content="Wspaniale"/>
            <ComboBoxItem Content="Dobrze"/>
            <ComboBoxItem Content="Neutralnie"/>
            <ComboBoxItem Content="Źle"/>
            <ComboBoxItem Content="Okropnie"/>
        </ComboBox>

        <CheckBox x:Name="ZrealizowaneCheckBox" Content="Zadanie zrealizowane" Margin="0,10,0,20"/>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Zapisz" IsDefault="True" Click="SaveButton_Click" Width="80" Margin="0,0,10,0" Style="{StaticResource AccentButton}"/>
            <Button Content="Anuluj" IsCancel="True" Width="80" Style="{StaticResource StandardButton}"/>
        </StackPanel>
    </StackPanel>
</Window>